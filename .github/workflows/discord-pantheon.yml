name: Discord notify - Pantheon

on:
  push:
    paths:
      - 'Pantheon/**'

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Post to Discord
        env:
          WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_PANTHEON }}
          REPO: ${{ github.repository }}
          BRANCH: ${{ github.ref_name }}
          ACTOR: ${{ github.actor }}
          MSG: ${{ github.event.head_commit.message }}
          URL: ${{ github.event.head_commit.url }}
          SHA: ${{ github.sha }}
        run: |
          payload=$(jq -n \
            --arg title "🛡️ Pantheon scripts updated" \
            --arg desc "$MSG" \
            --arg url "$URL" \
            --arg actor "$ACTOR" \
            --arg avatar "https://github.com/${ACTOR}.png" \
            --arg ahref "https://github.com/${ACTOR}" \
            --arg repo "$REPO" \
            --arg branch "$BRANCH" \
            --arg sha "${SHA:0:7}" \
            --arg ts "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            '{embeds: [{
              author: {name: $actor, url: $ahref, icon_url: $avatar},
              title: $title,
              description: $desc,
              url: $url,
              color: 10181046,
              timestamp: $ts,
              fields: [
                {name: "Repo",   value: $repo,   inline: true},
                {name: "Branch", value: $branch, inline: true},
                {name: "Commit", value: $sha,    inline: true}
              ],
              footer: {text: "isxScripts · GitHub"}
            }]}')
          curl -sf -H "Content-Type: application/json" -d "$payload" "$WEBHOOK"
